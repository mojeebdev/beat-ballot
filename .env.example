# Public site
NEXT_PUBLIC_APP_URL=https://beatballot.space

# The public build intentionally starts in preview mode. Switch this to "live"
# only after the Neon vote API and Managed Better Auth integration are enabled.
NEXT_PUBLIC_BALLOT_MODE=preview

# Neon (to be enabled in the next backend pass)
DATABASE_URL=
NEON_AUTH_BASE_URL=
NEON_AUTH_COOKIE_SECRET=
